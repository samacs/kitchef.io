<% content_for :title, t("runners.invalid.title") %>

<main class="mx-auto max-w-[480px] px-5 py-20 text-center">
  <span class="font-mono text-[11px] uppercase tracking-[0.18em] text-muted">
    <%= t("runners.invalid.kicker") %>
  </span>
  <h1 class="mt-3 font-serif text-[30px] leading-[1.15] text-ink">
    <%= t("runners.invalid.headline") %>
  </h1>
  <p class="mt-4 text-[14px] text-ink-2 leading-relaxed">
    <%= t("runners.invalid.sub") %>
  </p>
</main>
