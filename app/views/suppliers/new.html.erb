<% content_for :title, t("suppliers.new.title") %>
<% content_for :page_title, t("suppliers.new.title") %>
<% content_for :page_subtitle, t("suppliers.new.subtitle") %>

<div class="max-w-[640px] mx-auto">
  <div class="bg-surface border border-line rounded-panel shadow-card p-7 sm:p-9">
    <div class="flex flex-col gap-2 mb-6">
      <%= render Ui::KickerComponent.new(text: t("suppliers.new.kicker")) %>
      <h2 class="font-serif text-[28px] leading-[1.1] tracking-[-0.01em] text-ink">
        <%= t("suppliers.new.heading") %>
      </h2>
      <p class="text-[14px] text-ink-2 leading-[1.55] max-w-[46ch]">
        <%= t("suppliers.new.lede") %>
      </p>
    </div>

    <%= render "form", supplier: supplier %>
  </div>
</div>
