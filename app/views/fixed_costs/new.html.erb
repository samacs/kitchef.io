<% content_for :title, t("fixed_costs.new.title") %>
<% content_for :page_title, t("fixed_costs.new.title") %>
<% content_for :page_subtitle, t("fixed_costs.new.subtitle") %>

<div class="max-w-[640px] mx-auto">
  <div class="bg-surface border border-line rounded-panel shadow-card p-7 sm:p-9">
    <div class="flex flex-col gap-2 mb-6">
      <%= render Ui::KickerComponent.new(text: t("fixed_costs.new.kicker")) %>
      <h2 class="font-serif text-[28px] leading-[1.1] tracking-[-0.01em] text-ink">
        <%= t("fixed_costs.new.heading") %>
      </h2>
      <p class="text-[14px] text-ink-2 leading-[1.55] max-w-[46ch]">
        <%= t("fixed_costs.new.lede") %>
      </p>
    </div>

    <%= render "form", fixed_cost: fixed_cost %>
  </div>
</div>
