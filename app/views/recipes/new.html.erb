<% content_for :title, t("recipes.new.title") %>
<% content_for :page_title, t("recipes.new.title") %>
<% content_for :page_subtitle, t("recipes.new.subtitle") %>

<div class="max-w-[860px] mx-auto">
  <div class="bg-surface border border-line rounded-panel shadow-card p-7 sm:p-9">
    <div class="flex flex-col gap-2 mb-6">
      <%= render Ui::KickerComponent.new(text: t("recipes.new.kicker")) %>
      <h2 class="font-serif text-[28px] leading-[1.1] tracking-[-0.01em] text-ink">
        <%= t("recipes.new.heading") %>
      </h2>
      <p class="text-[14px] text-ink-2 leading-[1.55] max-w-[46ch]">
        <%= t("recipes.new.lede") %>
      </p>
    </div>

    <%= render "form", recipe: recipe %>
  </div>
</div>
