web: bin/rails server -b "ssl://0.0.0.0:${PORT:-3000}?key=${HTTPS_KEY_FILE}&cert=${HTTPS_CERT_FILE}"
css: bin/rails tailwindcss:watch
worker: bin/sidekiq -C config/sidekiq.yml
stripe: stripe listen --forward-to https://lvh.me:3000/webhooks/stripe
